% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_data.R
\name{prep_fia_data}
\alias{prep_fia_data}
\title{Prepare Raw FIA Data for Analysis}
\usage{
prep_fia_data(
  fia_trees,
  fia_plots = NULL,
  fia_cond = NULL,
  extra_tree_cols = NULL,
  extra_plot_cols = NULL,
  extra_cond_cols = NULL,
  include_dead = FALSE,
  variant = NULL
)
}
\arguments{
\item{fia_trees}{Data frame of FIA TREE table records. Must contain at
minimum: PLT_CN, SUBP, DIA, and STATUSCD.}

\item{fia_plots}{Optional data frame of FIA PLOT table records. Must
contain CN (joins to TREE.PLT_CN).}

\item{fia_cond}{Optional data frame of FIA COND table records. Must
contain PLT_CN and CONDID.}

\item{extra_tree_cols}{Character vector of additional TREE columns to keep.}

\item{extra_plot_cols}{Character vector of additional PLOT columns to keep.}

\item{extra_cond_cols}{Character vector of additional COND columns to keep.}

\item{include_dead}{Logical. When FALSE (default), filters to live trees
only (STATUSCD == 1). Set to TRUE to retain dead trees.}

\item{variant}{FVS variant code for species translation. If NULL, uses
the session variant set by \code{\link[=set_fvs_variant]{set_fvs_variant()}}.}
}
\value{
A tibble with standardized forestry column names and imperial units.
}
\description{
Maps raw FIA database column names to forestry-friendly names, joins
TREE + PLOT + COND tables, converts species codes, and keeps data in
\strong{imperial units} (DBH in inches, height in feet). This is a lower-level
function; most users will prefer \code{\link[=build_tree_list]{build_tree_list()}} which calls this
internally.
}
\examples{
mock_tree <- data.frame(
  PLT_CN = rep(1001, 4), SUBP = c(1, 1, 2, 3),
  DIA = c(10.2, 6.5, 8.0, 12.4), HT = c(55, 40, 48, 62),
  STATUSCD = c(1, 1, 1, 1), SPCD = c(12, 97, 261, 833)
)
prep_fia_data(mock_tree)

}
\seealso{
\code{\link[=build_tree_list]{build_tree_list()}}, \code{\link[=fia_to_forestry_names]{fia_to_forestry_names()}}
}
