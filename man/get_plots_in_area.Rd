% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{get_plots_in_area}
\alias{get_plots_in_area}
\title{Find FIA Plots Within an Area of Interest}
\usage{
get_plots_in_area(aoi, fia_data, buffer_ft = 0, data_dir = NULL)
}
\arguments{
\item{aoi}{One of:
\itemize{
\item File path to shapefile, GeoJSON, or GeoPackage
\item An \code{sf} object
\item A named numeric vector \code{c(xmin=, ymin=, xmax=, ymax=)} with a \code{crs} attribute
}}

\item{fia_data}{Either a list of FIA data frames (from \code{\link[=fetch_fia_data]{fetch_fia_data()}})
or a character vector of state abbreviations to auto-fetch.}

\item{buffer_ft}{Buffer distance in feet around the AOI. Default 0.}

\item{data_dir}{Directory for cached data (used when \code{fia_data} is state names).}
}
\value{
A list of FIA data frames filtered to only plots within the AOI.
Same structure as \code{\link[=fetch_fia_data]{fetch_fia_data()}} output.
}
\description{
Selects all FIA plot locations that fall within a given shapefile,
sf object, or bounding box. Uses Python geopandas/shapely for spatial
operations (auto-configured on first use).
}
\examples{
\dontrun{
# From a shapefile
fia_local <- get_plots_in_area("my_property.shp", fia_data = c("ME", "NH"))

# From an sf object
library(sf)
bbox <- st_as_sf(st_as_sfc(st_bbox(c(xmin=-70, ymin=44, xmax=-69, ymax=45),
                 crs = 4326)))
fia_local <- get_plots_in_area(bbox, fia_data = fia)
}

}
\seealso{
\code{\link[=plot_locations]{plot_locations()}}, \code{\link[=map_plots]{map_plots()}}, \code{\link[=fetch_fia_data]{fetch_fia_data()}}
}
