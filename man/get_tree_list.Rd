% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_list.R
\name{get_tree_list}
\alias{get_tree_list}
\title{Get Tree List from States or AOI (All-in-One)}
\usage{
get_tree_list(
  states = NULL,
  aoi = NULL,
  data_dir = NULL,
  include_dead = FALSE,
  variant = NULL,
  most_recent = TRUE
)
}
\arguments{
\item{states}{Character vector of state abbreviations. Required if
\code{aoi} is not provided.}

\item{aoi}{Area of interest (file path, sf object, or bbox). If provided,
filters plots to this area.}

\item{data_dir}{Directory for cached FIA data downloads.}

\item{include_dead}{Logical. Include standing dead trees? Default FALSE.}

\item{variant}{FVS variant code for species names.}

\item{most_recent}{Logical. Only the latest inventory cycle? Default TRUE.}
}
\value{
A tibble tree list. See \code{\link[=build_tree_list]{build_tree_list()}} for column details.
}
\description{
Convenience function that fetches FIA data, optionally filters by spatial
area, and returns a clean tree list. One-line solution for most use cases.
}
\examples{
\dontrun{
# From states
trees <- get_tree_list(states = "RI")

# From shapefile (auto-detects states from AOI extent)
trees <- get_tree_list(aoi = "my_property.shp", states = "ME")

# With dead trees
trees <- get_tree_list(states = "VT", include_dead = TRUE)
}

}
\seealso{
\code{\link[=build_tree_list]{build_tree_list()}}, \code{\link[=fetch_fia_data]{fetch_fia_data()}}, \code{\link[=get_plots_in_area]{get_plots_in_area()}}
}
